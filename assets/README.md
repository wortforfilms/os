# Assets

Tracked runtime assets for the QEMU alpha and desktop preview live here.

- `manifest.json` describes demo capsule assets.
- `demo.wasm` is a tiny placeholder payload for capsule wiring.
- `runtime/init` is the human-readable runtime bootstrap contract.

Raw HTML prototypes under `assets/html/` are intentionally local-only for now.
