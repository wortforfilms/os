# Kernel

Future home for the Rust-based Maataa OS kernel.

The current QEMU alpha kernel still lives at the repository root in `src/`,
`Cargo.toml`, `memory.x`, and the root build scripts. Moving it here should be a
separate migration with the alpha smoke gate preserved.
