# Lipi Runtime

Lipi Runtime is the Maataa / Vaigyaaniq script runtime scaffold for local-first script registry, glyph anchors, phonetic helpers, lineage graphing, learning paths, and PHKD evidence.

It is not production ready. The current release state is `GOVERNED_PRODUCTION_NO_GO`.
