import { createRuntimeSurface } from "../../components/status/RuntimeSurface";
export const SweepHistoryPanel = createRuntimeSurface("Sweep History", "Recent validation sweeps fade from skeleton to stats");
