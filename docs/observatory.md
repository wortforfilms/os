# Runtime Observatory

Observatory panels exist in `packages/maataa-ui/src/observatory`.

Current state: `STAGED`.

Blocked items:

- Route-level navigation to `/runtime-observatory`.
- Live SSE transport.
- Database-backed sweep history.
- Signed manifest ingestion into panels.
