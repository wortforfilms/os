# KBS API

The API and SDK contract exists in `packages/kbs-sdk`.

Current API maturity: `PREVIEW_VERIFIED`.

Remote production API deployment is blocked until auth hardening, account safety, rate limits, deployment evidence, and release governance are verified.
