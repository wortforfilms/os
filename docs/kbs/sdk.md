# KBS SDK

The TypeScript SDK supports a local in-memory client and an injectable transport boundary.

The Python SDK currently exposes a status contract only. It does not claim remote production readiness.
